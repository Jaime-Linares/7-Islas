import AppNavbar from "./AppNavbar";
import { render, screen } from "./test-utils";

describe('AppNavbar', () => {

    /*
    test('Muestra los public link corrrectamente', () => {
        render(<AppNavbar />);
        const linkDocsElement = screen.getByRole('link', { name: 'Register' });
        expect(linkDocsElement).toBeInTheDocument();

        const linkPlansElement = screen.getByRole('link', { name: 'Login' });
        expect(linkPlansElement).toBeInTheDocument();
    });

    test('Muestra los links de admin correctamente', () => {
        render(<AppNavbar />);
        const linkUsersElement = screen.getByRole('ADMIN', { name: 'Users' });
        expect(linkUsersElement).toBeInTheDocument();
        const linkPlayersElement = screen.getByRole('ADMIN', { name: 'Players' });
        expect(linkPlayersElement).toBeInTheDocument();
        const linkListGamesAElement = screen.getByRole('ADMIN', { name: 'Games' });
        expect(linkListGamesAElement).toBeInTheDocument();
        const linkAchievementsElement = screen.getByRole('ADMIN', { name: 'Achievements' });
        expect(linkAchievementsElement).toBeInTheDocument();
    });

    test('Muestra los links de player correctamente', () => {
        render(<AppNavbar />);
        const linkHomeElement = screen.getByRole('PLAYER', { name: 'Home' });
        expect(linkHomeElement).toBeInTheDocument();
        const linkStatsElement = screen.getByRole('PLAYER', { name: 'Stats' });
        expect(linkStatsElement).toBeInTheDocument();
        const linkAchievementsElement = screen.getByRole('PLAYER', { name: 'Achievements' });
        expect(linkAchievementsElement).toBeInTheDocument();
        const linkFriendsElement = screen.getByRole('PLAYER', { name: 'Friends' });
        expect(linkFriendsElement).toBeInTheDocument();
        const linkGamesElement = screen.getByRole('PLAYER', { name: 'Games' });
        expect(linkGamesElement).toBeInTheDocument();
    });
*/
});
